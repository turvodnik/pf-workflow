# tests/known-failures-T017.txt — pf-workflow allowlist for known-upstream-
# fixed-but-not-yet-synced test failures (mechanism added T-016/F-14).
#
# Emptied 2026-08-13: T-017 synced canon into this repo, closing every case
# this file used to track (codex-review.sh safety fixes T-008, strict-YAML
# frontmatter quoting T-009, <BASE> -> PF_AUTO_BASE T-014/F-15). Verified as
# a real fix, not just "accounted for": the underlying harnesses run fully
# green on their own (codex-review-harness 59/59, yaml-strict 9/9,
# base-exec 2/2), not merely matched against this list. Kept as an empty
# file, not deleted — tests/run.sh's ALLOWLIST path is unconditional and the
# mechanism may be needed again.
#
# If a future canon fix lands upstream before the next sync reintroduces
# this gap, list the resulting case here again, one line per case:
# <section><TAB><normalized case label> — section is one of
# codex-review-harness / yaml-strict / base-exec. Case labels containing
# pid=NNNN or elapsed=Ns are normalized by run.sh (pid=N / elapsed=Ns)
# before comparing, since those two numbers are not deterministic run to
# run. Do not add a line to silence a failure you have not root-caused as
# "already fixed upstream, sync pending" — that defeats the point of this
# file. run.sh turns RED on either a failure not listed here (unexpected
# regression) or a listed line that now passes (stale entry — delete it).
